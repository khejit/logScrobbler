name: Build and Deploy

on:
  push:
    branches:
      - rewrite

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v2.3.1

    - name: Build
      run: |
        npm install
        npm run build
      env:
        VUE_APP_LASTFM_API_KEY: ${{ secrets.LastfmApiKey }}
        VUE_APP_LASTFM_API_SECRET: ${{ secrets.LastfmApiSecret }}

    - name: Deploy
      uses: JamesIves/github-pages-deploy-action@4.1.0
      with:
        branch: gh-pages
        folder: dist
